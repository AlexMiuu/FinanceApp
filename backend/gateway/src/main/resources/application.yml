server:
  port: 8080

spring:
  application:
    name: gateway

auth:
  jwks-uri: ${AUTH_JWKS_URI:http://localhost:8081/api/v1/auth/jwks}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
